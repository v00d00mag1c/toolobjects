Submodules are the links between classes that allows to logically define how objects connected between themselves. They sums recursively from mro items.

They have role= that is used in some cases (for example, Wheel)
