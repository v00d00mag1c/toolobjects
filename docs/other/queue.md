Queue is a class that allows to define run queue of some kind of executables and return in some format. There is "prestart" and "items" with QueueItem items. 

predicate - name of the executable

arguments - values for "execute()" function

build - constructor arguments

direct_value, output...
