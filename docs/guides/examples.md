**Getting random number and saving it**

```
> python tool.py -i Data.Random.GetRandomInt.GetRandomInt -save_to tmp
```

**Searching**

```
> python tool.py -i App.DB.Search -storage common
```

**Change name of item**

```
> python tool.py -i App.Objects.Operations.Edit -object {uuid} -key obj,name -val comedy
```
