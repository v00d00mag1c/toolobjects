Bifurcation executable, takes submodules with role=wheel and if it meets with at least one passed arg, uses got submodule s execute().

If needed submodule not found, fallbacks to implementation of self that throws AssertionError by default.
