### File.FileTypes.FileType

Type: `Object`

I can say, extended File. Abstract class that represents file with some type. Contains `file` or `storage_unit` field. Real file can be get with `get_file()` function.
