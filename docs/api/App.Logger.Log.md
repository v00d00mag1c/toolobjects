### Log

`message` (str) — text of the message

`role` (list of str) — internal roles of the object. some values changes color

`time` (datetime) — time when log was created

`section` (LogSection) — object where log was taken

`prefix` (LogPrefix) — additional ID of message
