### App.Console.Interactive.Interactive

It's a [Console](App.Console.Console.md), but it does not stops after first call. It will be useful, because the app takes a long time to start.

You can exit it via Ctrl+C or `-i App.Console.Interactive.Exit`.
