### Storage

Storage is represnted via StorageItems. Each StorageItem has DB adapter and storage adapter. Files are represented via StorageUnits.

Option that contains list of storageitems is `storage.items`. It must contain unique name.
